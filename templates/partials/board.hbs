<h2>Leaderboard</h2>
<div class="leader-nav">
	<button class="btn active" data-subsection="individuals">Individual</button>
	<button class="btn" data-subsection="teams">Teams</button>
</div>

<div id="individual-leader" class="subsection board individuals ACTIVE">
	<ul>
		{{#each individuals}}
		<li>{{listbump @index}}. <div class="points">{{leader}}</div> {{{name.first}}} <span class="team">{{team.name}}</span></li>
		{{/each}}
	</ul>

</div>

<div id="team-leader" class="subsection teams board">
	{{#if player.team}}
	Let's see how team <strong>{{player.team.name}}</strong> is doing!
	{{else}}

	Looks like you don't have a team - you should pick one in your profile settings! 

	{{/if}}

	<ul>
		{{#each teams}}
		<li>{{listbump @index}}. <div class="points">{{this.score}}</div> {{this.team}}</li>
		{{/each}}
	</ul>

</div>

<button class="back nav-bar right" data-section="game">back</button>